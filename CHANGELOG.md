# CHANGELOG

## v0.4.1 - 2026-08-11

### Added

- 增加固定 11×11 诊断模式、外特性/Map/总耗时统计、逐转速及诊断逐点日志。
- 增加参数校验、外特性、内部 Map、损耗/效率、图形数据刷新的真实阶段进度，显示已完成点数、总点数、百分比和总耗时。

### Changed

- 将“电感模型”合并到“电机参数”折叠组；固定模式隐藏 Map 控件，饱和模式按需显示单位、导入、预览和范围。
- Ld/Lq Map 预览使用蓝—青—绿—黄—红高对比连续色图，Ld/Lq 独立自动缩放色阶、μH 色条和 Id/Iq/电感悬停信息。

### Fixed

- 后台 worker 最外层结构化回传异常类型、文件和行号，完整 traceback 写日志；失败/取消后恢复按钮并保留旧结果。
- 移除 dq 刷新阶段在 GUI 主线程重新搜索完整 MTPA/MTPV 轨迹导致的长时间假死。
- 求解器统一有限 `maxiter`、超时失败出口及超过 5 秒的 WARNING 记录字段。

### Performance

- 内部 Map 复用完全一致转速网格的外特性边界，禁止插值复用，避免默认网格重复求解。
- Map 已求得的 MTPA/MTPV 参考点持久到结果数据，dq 曲线和当前转速 MTPV 直接复用；饱和 11×11 GUI 刷新由约 162 s 降至约 0.51 s。

### Known Issues

- 饱和电感正式 81×121 仍是高计算量任务；按 11×121 密度诊断估算约 5–6 分钟，但已保持 GUI 响应、可查看进度并可取消。
- 尚未支持 Ψd/Ψq 磁链 Map、差分电感项和开绕组共模/环流模型。

## v0.4.0 - 2026-08-11

- 新增 `CONSTANT` 与 `SATURATION_MAP` 电感模型；支持从含 Ld/Lq 工作表的 Excel 导入、预览、双线性插值及 Map 范围校验。
- MTPA、MTPV、弱磁、外特性、内部工况 Map、电压和损耗计算统一使用当前工作点的 Ld(Id,Iq)/Lq(Id,Iq)。
- 新增星接、角接和开绕组接法；开绕组支持共母线、隔离母线及自定义双 Udc。
- 当前工作点增加 Ld/Lq 显示，项目 JSON 兼容保存饱和 Map 与接线拓扑。
- 新增案例保存、加载、删除和多选对比；支持外特性/dq 叠加、Efficiency Map、ΔEfficiency 与 ΔTorque。
- 工况 Map 算法升级为 `operating-map-3.0`，并补充饱和模型、接线换算和案例仓库回归测试。

## v0.3.0 - 2026-07-25

- 效率 Map 改为深蓝—蓝—青—绿—黄—橙—红连续色图，色条使用百分比。
- 外特性、Map 和 dq 图新增深灰加粗零轴，普通网格保持浅灰细线。
- 统一 MTPA、弱磁、MTPV、外特性和当前选中点的颜色与散点样式。
- 外特性图例固定左上，并增加顶部绘图区余量；dq 图例固定右上。
- 新增版本化 release 自动构建、完整测试、包内冒烟测试、ZIP 和 SHA256。
- 新增根目录 `PROJECT_HANDOFF.md`，用于低 token 成本交接。

## v0.2.0

- 实现两种 Udc 输入模式、MTPA/MTPV、内部工况 Map、损耗效率和点击联动。
- 取消参数变化后的自动完整计算，改为后台手动计算和结果过期提示。
- 内部 Map 增加按点数/按步长网格，默认正式计算为 81×121。
