# CHANGELOG

## v0.5.0 - 2026-08-11

### Added

- 增加可复现的CONSTANT/SATURATION_MAP、11×11/81×121性能benchmark，统计外特性、Map、总耗时、单点均值/最大值、物理函数元素数、优化器次数和失败数。
- 增加跨转速同转矩及同转速相邻转矩热启动，并在诊断日志记录`warm_start`状态。
- 增加统一`NoWheelSpinBox`/`NoWheelDoubleSpinBox`及滚轮、键盘、Tab、页面滚动回归测试。
- 主窗口顶部新增固定操作栏，集中显示开始、取消、真实进度、阶段、完成点数和用时。

### Changed

- 饱和Map在构造时一次创建并缓存只读轴、矩阵及分母数组，后续插值不再重复校验和复制。
- 饱和外特性改为Map有效域内分层一维搜索并复用邻近转速Id；饱和参考点改为批量局部细化，取消逐标量SciPy反复重建转矩根。
- 内部Map的饱和定转矩根使用与物理容差匹配的32轮二分、有效域参考网格、行级参考缓存和分层局部细化；窄边界漏点单独回退高密度网格。
- 所有数值输入框隐藏上下箭头且忽略鼠标滚轮，页面滚动仍保留；程序入口默认使用Qt`showMaximized()`。

### Fixed

- 避免饱和求解在Map范围外生成大量必为NaN的候选点。
- 避免外特性每个转速通过数百次标量目标调用重复创建插值数组和转矩根。
- 参数修改后顶部操作栏明确显示“参数已修改，结果已过期”。

### Performance

- 同机饱和81×121由342.97 s降至27.44 s（12.5×）；外特性41.92→3.70 s，内部Map301.05→23.74 s。
- 饱和81×121平均/最大单点30.72/43.30 ms降至2.42/3.17 ms；电感插值元素14.44亿降至3.25亿，失败数保持1不变。
- 固定电感81×121保持约1.8 s；优化未减少网格、放宽约束或更改物理方程。

### Known Issues

- benchmark饱和81×121仍有1个原有不可行内部请求，与v0.4.2基线一致，不是新增数值失败。
- 饱和Map仍以双线性Ld/Lq和固定ψf建模；尚未支持Ψd/Ψq磁链Map、差分电感和开绕组共模/环流。

## v0.4.2 - 2026-08-11

### Added

- 左侧主按钮下方新增常驻可见的独立计算状态卡片，显示真实阶段、0～100%进度、已完成/总点数、用时和取消按钮。
- 饱和电感模式新增Excel格式提示、格式示例弹窗及可编辑的Ld/Lq双Sheet空白模板导出。
- 新增饱和工作簿缺失Sheet、轴不一致、空白单元格和非数值单元格的精确错误提示。
- 新增100%/125%界面缩放截图验证工具和UI交互回归测试。

### Changed

- 饱和Map模式真正隐藏固定Ld/Lq输入行，切回固定模式时恢复显示；当前工作点仍显示插值得到的Ld/Lq。
- 统一浅灰窗口、白色卡片、蓝色强调、32px输入框、圆角边框及主/次/取消按钮的现代工程软件样式。
- 计算阶段明确为参数校验、外特性、MTPA/MTPV、内部工作点、损耗/效率Map和图形刷新。

### Fixed

- 修复取消计算时提前递增请求版本，导致取消回调被忽略、界面可能残留“正在计算”的问题。
- 参数在计算期间变化或线程提前结束时，进度卡片现在会可靠退出运行态。
- 无可行内部点、后台异常或取消时保留上一次完整结果，不再用失败结果覆盖。

### Performance

- 本版仅优化UI与交互，不改变已验证的电机方程、约束和求解结果口径。

### Known Issues

- 饱和电感正式81×121仍属于高计算量任务，预计约5～6分钟；可通过真实进度观察并随时取消。
- 发布包未进行代码签名；开绕组共模/环流和Ψd/Ψq磁链Map仍未建模。

## v0.4.1 - 2026-08-11

### Added

- 增加固定 11×11 诊断模式、外特性/Map/总耗时统计、逐转速及诊断逐点日志。
- 增加参数校验、外特性、内部 Map、损耗/效率、图形数据刷新的真实阶段进度，显示已完成点数、总点数、百分比和总耗时。

### Changed

- 将“电感模型”合并到“电机参数”折叠组；固定模式隐藏 Map 控件，饱和模式按需显示单位、导入、预览和范围。
- Ld/Lq Map 预览使用蓝—青—绿—黄—红高对比连续色图，Ld/Lq 独立自动缩放色阶、μH 色条和 Id/Iq/电感悬停信息。

### Fixed

- 后台 worker 最外层结构化回传异常类型、文件和行号，完整 traceback 写日志；失败/取消后恢复按钮并保留旧结果。
- 移除 dq 刷新阶段在 GUI 主线程重新搜索完整 MTPA/MTPV 轨迹导致的长时间假死。
- 求解器统一有限 `maxiter`、超时失败出口及超过 5 秒的 WARNING 记录字段。

### Performance

- 内部 Map 复用完全一致转速网格的外特性边界，禁止插值复用，避免默认网格重复求解。
- Map 已求得的 MTPA/MTPV 参考点持久到结果数据，dq 曲线和当前转速 MTPV 直接复用；饱和 11×11 GUI 刷新由约 162 s 降至约 0.51 s。

### Known Issues

- 饱和电感正式 81×121 仍是高计算量任务；按 11×121 密度诊断估算约 5–6 分钟，但已保持 GUI 响应、可查看进度并可取消。
- 尚未支持 Ψd/Ψq 磁链 Map、差分电感项和开绕组共模/环流模型。

## v0.4.0 - 2026-08-11

- 新增 `CONSTANT` 与 `SATURATION_MAP` 电感模型；支持从含 Ld/Lq 工作表的 Excel 导入、预览、双线性插值及 Map 范围校验。
- MTPA、MTPV、弱磁、外特性、内部工况 Map、电压和损耗计算统一使用当前工作点的 Ld(Id,Iq)/Lq(Id,Iq)。
- 新增星接、角接和开绕组接法；开绕组支持共母线、隔离母线及自定义双 Udc。
- 当前工作点增加 Ld/Lq 显示，项目 JSON 兼容保存饱和 Map 与接线拓扑。
- 新增案例保存、加载、删除和多选对比；支持外特性/dq 叠加、Efficiency Map、ΔEfficiency 与 ΔTorque。
- 工况 Map 算法升级为 `operating-map-3.0`，并补充饱和模型、接线换算和案例仓库回归测试。

## v0.3.0 - 2026-07-25

- 效率 Map 改为深蓝—蓝—青—绿—黄—橙—红连续色图，色条使用百分比。
- 外特性、Map 和 dq 图新增深灰加粗零轴，普通网格保持浅灰细线。
- 统一 MTPA、弱磁、MTPV、外特性和当前选中点的颜色与散点样式。
- 外特性图例固定左上，并增加顶部绘图区余量；dq 图例固定右上。
- 新增版本化 release 自动构建、完整测试、包内冒烟测试、ZIP 和 SHA256。
- 新增根目录 `PROJECT_HANDOFF.md`，用于低 token 成本交接。

## v0.2.0

- 实现两种 Udc 输入模式、MTPA/MTPV、内部工况 Map、损耗效率和点击联动。
- 取消参数变化后的自动完整计算，改为后台手动计算和结果过期提示。
- 内部 Map 增加按点数/按步长网格，默认正式计算为 81×121。
